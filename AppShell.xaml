<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Punjab_Ornaments.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Punjab_Ornaments"
    xmlns:Page="clr-namespace:Punjab_Ornaments.Presentation.Views"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
        <Tab Title="Home Page">
            <ShellContent Title="Home" ContentTemplate="{DataTemplate Page:HomePageView}" Route="MainPage" />
        </Tab>
        <Tab Title="Approval">
            <ShellContent Title="Home" ContentTemplate="{DataTemplate Page:HomePageView}" Route="MainPage" />
        </Tab>
        <Tab Title="Settings">
            <ShellContent Title="Home" ContentTemplate="{DataTemplate Page:HomePageView}" Route="MainPage" />
        </Tab>
    </TabBar>


</Shell>
